prototype(Futape.NodeTypes:Image) < prototype(Neos.Fusion:Value) {
	value = Futape.Bootstrap:Image {
		style = ${q(node).property('style')}

		attributes {
			src = Neos.Neos:ImageUri {
				asset = ${q(node).property('image')}
			}
		}
	}
}
