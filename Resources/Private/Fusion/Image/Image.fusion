prototype(Futape.NodeTypes:Image) < prototype(Futape.Bootstrap:Image) {
	style = ${q(node).property('style')}

	src = Neos.Neos:ImageUri {
		asset = ${q(node).property('image')}
	}

	alt = ${q(node).property('alt')}
}
